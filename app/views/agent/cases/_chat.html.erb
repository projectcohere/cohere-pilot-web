<div class="CaseDetail-phone Phone-frame">
  <%=
    render(partial: "chats/chat", locals: {
      chat: chat,
      sender: Chat::Sender.agent(cookies.signed[:chat_user_id]),
      receiver: recevier,
      data: {
        id: chat.id.val,
      },
    })
  %>

  <div class="Chat-agent">
    <%= render(partial: "chats/macros") %>

    <% case @demo_id %>
    <% when "n04" %>
      <%= demo_coachmark_tag(t("demo.role.nonprofit.mark")[3], anchor: :left) %>
    <% end %>
  </div>
</div>

<% case @demo_id %>
<% when "n03" %>
  <%= demo_coachmark_tag(t("demo.role.nonprofit.mark")[2], anchor: :left) %>
<% end %>
