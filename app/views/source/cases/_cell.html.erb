<%= tag.li(case_cell_options(cell)) do %>
  <div class="CaseCell-title">
    <%=
      link_to(
        cell.recipient_name,
        cell.show_path,
        class: "CaseCell-name",
      )
    %>

    <% if not cell.shows_assign? %>
      <p class="CaseCell-assignee">
        <%= cell.assignee_email %>
      </p>
    <% end %>
  </div>

  <div class="CaseCell-details">
    <p class="CaseCell-organizations">
      <%= cell.details_names %>
    </p>
  </div>

  <div class="CaseCell-footer">
    <p class="CaseCell-status">
      <%= cell.status_name %>
    </p>

    <p class="CaseCell-date">
      <%= cell.created_label %>
    </p>
  </div>
<%- end %>

<% if @demo_id == "c05" && cell.id.val == 1 %>
  <%= demo_coachmark_tag(t("demo.role.call-center.mark")[4], anchor: :right) %>
<% end %>
